# Obrigatório
KB_READER_PASSWORD=senha_aqui
QDRANT_URL=http://localhost:6335

# Opcional
QDRANT_API_KEY=
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_DB=knowledge_base
POSTGRES_USER=kb_reader
RUST_LOG=info

# "production" → rejeita QDRANT_URL http://, ativa gate de aprovação humana
NEXUS_ENV=development

# Deixe vazio para usar o padrão (~/.cache/fastembed_cache)
# Aponte para diretório local em ambientes air-gapped
FASTEMBED_CACHE_PATH=
